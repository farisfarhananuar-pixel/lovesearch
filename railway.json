{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "chmod -R 775 storage bootstrap/cache && php artisan package:discover --ansi && php artisan config:clear && php artisan view:clear && php artisan migrate --force && php artisan storage:link --force && /start-container.sh"
  }
}
